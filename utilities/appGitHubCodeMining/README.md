# GitHub Code Mining Updated (Unified)

## Features

<!-- - Multi LLM + Unified Prompting System
- Deep Search: Semantic + Structural + Contextual Code Search
- Agent Planner → decides search strategy
- Unified Scorer → ranks results
- FastAPI server → `/api/repo_search`, `/api/deep_search` and static `/web`
- CLI client to test -->

## Usage

<!-- 1. Install requirements

```
pip install -r requirements.txt
```

2. Start the server (from repo root)

```
uvicorn Utilities.GitHubCodeMiningUpdated.server.api:app --reload
```

3. Run the client

```
python client/client.py
```

4. Open <http://127.0.0.1:8000/web> and try Repository vs Deep modes 🚀 -->
